# C++ core lesson depth review — 2026-09-11

Expanded all 22 core modules in 11 C++ subunits against baseline commit `bf3f048`. The scope excludes Basics and the six advanced C++ subunits. Read `notes/modules.md` and the current curriculum evidence policy before authoring. The purpose is a self-contained explanation with sequenced practice, not a compressed outline or a claim to cover every C++ nuance.

Explanatory text grew from **9,737 to 25,053 words** across these modules; individual lessons contain **1,098–1,174 words**. Counts include paragraph/callout text and reflection prompts/solutions, excluding code, citations, headings and objectives. Each lesson retains three navigable parts and now has **three explained reflections** (66 total), with one checkpoint in each part.

All **22 original complete C++20 programs**, their immediately following expected-output blocks, and every other existing code block remain byte-for-byte unchanged as JSON values. The new contrasting scenarios are explained in original prose; they are not incomplete executable snippets. No new arbitrary-input, scheduler, empirical-performance or external-platform guarantee is inferred from a finite teaching example.

## Per-module teaching and evidence review

| Module | Words before → after | Walkthrough | Contrasting worked scenario | Additional practice | Evidence inspected or retained |
| --- | ---: | --- | --- | --- | --- |
| [Initialize values and read deduced types](../../content/units/cpp/types-initialization/01-initialization-and-deduction.json) | 451 → 1162 | Two integer objects and an alias; exact vector constructor interpretation. | Four zero counters versus two listed integers; owner copy versus reference. | Aliasing cannot rebind; replacing constructor parentheses changes the requested collection. | dcl.init, dcl.init.list, dcl.spec.auto; freshly inspected vector.cons/3–8 and dcl.init.ref/1–2. |
| [Conversions and domain types](../../content/units/cpp/types-initialization/02-conversions-and-domain-types.json) | 445 → 1131 | Convert before division and retain application units. | Signed fractional truncation, magnitude versus precision, identity types. | Conversion placement and distinct identifier classes. | conv.fpint freshly inspected; existing conv.integral, dcl.enum, dcl.typedef and expr.mul contracts retained. |
| [Parameters, results, and borrowing](../../content/units/cpp/functions-interfaces/01-parameters-and-results.json) | 446 → 1163 | Accumulator states 0, 4, 10, 15; nested call result; owner return. | Returning a local reference versus an owned report or documented argument borrow. | Empty inputs/overflow policy and deferred borrowed arguments. | Fresh dcl.init.ref; existing expr.call and basic.life support parameter/result/lifetime reasoning. |
| [Overloads and local callable objects](../../content/units/cpp/functions-interfaces/02-overloads-and-lambdas.json) | 468 → 1164 | Candidate conversion ranking and closure snapshot creation before calls. | Snapshot threshold versus a live setting; retained reference and pointer capture limits. | Float/double overloads and stable batch policy. | Fresh over.ics.rank, expr.prim.lambda.capture, dcl.spec.auto and added conv.fpprom. |
| [Classes that maintain a useful invariant](../../content/units/cpp/classes-value-semantics/01-invariants-and-operations.json) | 444 → 1127 | Constructor establishes (10,0); accepted and rejected transitions. | Negative normalization, accepted zero request and overflow-prone alternate guard. | Exact completion/no-op/failure and unsafe setter design. | Fresh class.base.init; existing class.ctor and class.copy.ctor plus explicitly derived finite arithmetic. |
| [Copying, moving, and comparing values](../../content/units/cpp/classes-value-semantics/02-copy-move-and-comparison.json) | 443 → 1149 | Four records and independent value changes; no guessed moved-from string. | Cache exclusion from equality and noncopyable owner members. | Semantic equality and implicit move suppression by a user-declared destructor. | Fresh class.copy.ctor and class.eq; added unique.ptr.single synopsis; existing member assignment/moved-from rules. |
| [Owners, borrowers, and scope](../../content/units/cpp/lifetime-ownership/01-owners-and-borrowers.json) | 438 → 1154 | Owner/view arrows; first five title characters and live changed vector element. | Append with reallocation versus no reallocation and the old span’s unchanged length. | View constness and invalidation versus stale extent. | Fresh span.overview and vector.modifiers; existing basic.life/string.view.template retained. |
| [RAII and exclusive ownership](../../content/units/cpp/lifetime-ownership/02-raii-and-unique-ownership.json) | 439 → 1157 | Owner state before/after move and inner-scope destruction. | Early-return cleanup and a raw observer through ownership transfer. | Object address versus owner transfer; selecting a local value or dynamic owner. | Fresh unique.ptr.single.ctor/16–20; existing class.dtor, unique.ptr, forward and Guidelines guidance retained. |
| [Optional results and complete parsing](../../content/units/cpp/error-handling/01-optional-results-and-parsing.json) | 420 → 1134 | Success code, stopping pointer and domain validation for 24 and 12x. | Input matrix with -3, +3, 003, -0, whitespace and out-of-range digits. | Layered rejection reasoning and preserving error information. | Fresh charconv.from.chars/1–5 and optional.observe; C++23 expected remains later-standard discussion only. |
| [Exceptions and state guarantees](../../content/units/cpp/error-handling/02-exceptions-and-state-guarantees.json) | 436 → 1125 | Candidate copy, validation, rejection cleanup and swapped ownership. | In-place partial update and aliased input/target. | Post-swap old-state cleanup and noexcept versus recoverability. | Fresh except.ctor and vector.capacity; existing default.allocator and except.spec; scoped state guarantee. |
| [Templates as families of typed operations](../../content/units/cpp/templates-generic-programming/01-typed-families.json) | 457 → 1157 | Concrete int/string specializations and owning Box results. | Explicit double/int choices for mixed inputs and left-selection tie policy. | Mixed conversion policies and copy requirement versus borrowed result. | Fresh temp.deduct.call and temp.inst; existing temp.param supports type families. |
| [Concepts and explicit requirements](../../content/units/cpp/templates-generic-programming/02-concepts-and-requirements.json) | 428 → 1098 | Unevaluated const expression checks and runtime counts five/three. | Mutable-only size member and negative count sentinel. | Matching required constness and avoiding unrelated capabilities. | Fresh expr.prim.req and concept.convertible; existing temp.constr; semantic conditions remain distinct from structural checks. |
| [Sequence containers and invalidation](../../content/units/cpp/library-data-containers/01-sequences-and-invalidation.json) | 445 → 1141 | Empty → reserve → two appends → erase → justified front access. | Erasing before a saved iterator versus a bounds-valid index with changed identity. | Size/capacity and old end invalidation. | Fresh vector.capacity and vector.modifiers; existing vector.overview; actual element lifetime distinct from spare allocation. |
| [Keys, records, and alternative values](../../content/units/cpp/library-data-containers/02-keys-and-vocabulary-types.json) | 463 → 1174 | Orbits/Atlas aggregation, ordered output and get_if success. | Absent versus known-zero lookup and string alternative access. | Repeated-key counts and record/optional/variant selection. | Fresh map.access and added variant.get/7–10; existing associative/unordered requirements and structured bindings. |
| [Algorithms and half-open ranges](../../content/units/cpp/iterators-algorithms-ranges/01-algorithms-and-half-open-ranges.json) | 445 → 1118 | Sorted positions, checked search and independent even count. | Missing and empty search, duplicates and logical identity after sorting. | Segment-specific end and invalid non-strict comparator. | Fresh alg.find and alg.count; existing iterator.requirements.general and alg.sorting; no measured cost claims. |
| [Lazy range pipelines](../../content/units/cpp/iterators-algorithms-ranges/02-lazy-range-pipelines.json) | 440 → 1141 | Alternating rejected/accepted values and explicit materialization. | Reversing transform/filter order changes outputs; workload-sensitive materialization. | No matches and owned return versus retained borrow. | Fresh range.filter begin caching and range.transform iterator dereference; source kept stable. |
| [Text streams and extraction state](../../content/units/cpp/library-services/01-text-streams-and-extraction.json) | 443 → 1144 | Input position, required extraction, ws and full record. | Surplus data, domain failure, malformed integer and EOF before ws. | Input classification and redesign for spaced titles. | Fresh istream.manip and istream.sentry; existing string.streams/formatted requirements; no file durability claim. |
| [Time durations and filesystem paths](../../content/units/cpp/library-services/02-time-and-path-values.json) | 425 → 1124 | 2500ms arithmetic, integral-second conversion and lexical components. | Coverage rounding policy and leading parent pathname. | cast/floor/ceil/round ties and lexical containment limits. | Fresh added time.duration.cast and time.point; fs.path.generic normalization steps; steady_clock remains existing source. |
| [Declarations, definitions, and linkage](../../content/units/cpp/program-organization/01-declarations-and-linkage.json) | 445 → 1130 | Source-order visibility versus runtime calls; internal helper. | Header/client/implementation arrangement, missing definition and duplicated body. | Compile-only versus final link and independent local helpers. | Fresh basic.link and basic.def.odr/10–17; existing GCC compilation-stage documentation retained. |
| [Headers, namespaces, and module boundaries](../../content/units/cpp/program-organization/02-headers-and-modules.json) | 451 → 1121 | Inline configuration, owned description and local namespace alias. | Guarded ordinary definition in two units and inconsistent inline bodies. | Self-sufficient header and module migration boundary. | Fresh dcl.inline and basic.def.odr/13; existing cpp.include/module.unit/global fragment/GCC guard documentation retained. |
| [Interfaces and virtual dispatch](../../content/units/cpp/runtime-polymorphism/01-interfaces-and-overriding.json) | 436 → 1107 | Static Notice access with two live concrete objects and literal views. | Missing const override and a correct dispatch returning dangling data. | Derived-only members and constructor/destructor dispatch limits. | Fresh class.virtual and class.cdtor; existing class.abstract, class.dtor and expr.delete. |
| [Owning a collection of implementations](../../content/units/cpp/runtime-polymorphism/02-owning-polymorphic-objects.json) | 429 → 1132 | Transferred owners, Welcome failed cast and Reminder day query. | Owner-vector reallocation versus erasure of an owned object. | Two handle layers and empty-owner dereference policy. | Fresh expr.dynamic.cast, expr.delete; added vector.modifiers and unique.ptr.single.ctor; existing virtual/deletion contracts. |

## Focused source additions

Added 10 module-specific references. Existing bibliography records and locators were preserved. All added references use pinned N4861 committee wording and identify it as a C++20 working draft, not a purchased published ISO edition. Relevant passages were freshly opened before use:

- [C++20 working draft N4861: vector.cons](https://timsong-cpp.github.io/cppwp/n4861/vector.cons) — N4861 [vector.cons]; paragraphs 3–8, element-count and count-and-value construction; consulted 2026-09-11.
- [C++20 working draft N4861: dcl.init.ref](https://timsong-cpp.github.io/cppwp/n4861/dcl.init.ref) — N4861 [dcl.init.ref]; paragraphs 1–2, initialization and non-rebinding of references; consulted 2026-09-11.
- [C++20 working draft N4861: conv.fpprom](https://timsong-cpp.github.io/cppwp/n4861/conv.fpprom) — N4861 [conv.fpprom]; paragraphs 1–2, float-to-double promotion used in overload ranking; consulted 2026-09-11.
- [C++20 working draft N4861: unique.ptr.single](https://timsong-cpp.github.io/cppwp/n4861/unique.ptr.single) — N4861 [unique.ptr.single]; class synopsis, deleted copy constructor and copy assignment; consulted 2026-09-11.
- [C++20 working draft N4861: unique.ptr.single.ctor](https://timsong-cpp.github.io/cppwp/n4861/unique.ptr.single.ctor) — N4861 [unique.ptr.single.ctor]; paragraphs 16–20, exclusive ownership transfer and empty source after move; consulted 2026-09-11.
- [C++20 working draft N4861: variant.get](https://timsong-cpp.github.io/cppwp/n4861/variant.get) — N4861 [variant.get]; paragraphs 7–10, get_if returns null for a nonmatching alternative; consulted 2026-09-11.
- [C++20 working draft N4861: time.duration.cast](https://timsong-cpp.github.io/cppwp/n4861/time.duration.cast) — N4861 [time.duration.cast]; duration_cast scaling and floor/ceil/round contracts, including ties-to-even rounding; consulted 2026-09-11.
- [C++20 working draft N4861: time.point](https://timsong-cpp.github.io/cppwp/n4861/time.point) — N4861 [time.point]; time_point clock/epoch model and non-member subtraction of compatible time points; consulted 2026-09-11.
- [C++20 working draft N4861: vector.modifiers](https://timsong-cpp.github.io/cppwp/n4861/vector.modifiers) — N4861 [vector.modifiers]; paragraph 1, reallocation invalidation for references to vector owner elements; consulted 2026-09-11.
- [C++20 working draft N4861: unique.ptr.single.ctor](https://timsong-cpp.github.io/cppwp/n4861/unique.ptr.single.ctor) — N4861 [unique.ptr.single.ctor]; paragraphs 16–24, moves transfer stored pointers and clear source owners; consulted 2026-09-11.

## Validation and boundaries

- Scoped `npm run content:check -- cpp` passed after expansion. It validates the whole C++ topic’s 19 unit records, 41 modules and 123 parts; structural validity alone is not factual certification.
- A separate comparison against HEAD confirmed unchanged stable module/part IDs, objectives, discovery metadata, version IDs/numbers, part titles, original code and output values, and original source records in all 22 owned modules.
- Reviewed conversion examples, count/capacity traces, input-classification cases, alternative states, parameter and owner lifetimes, generic-expression requirements, interface dispatch and definition consistency. Contrasting traces are original deductions from the cited contracts.
- The root integration task runs all executable examples and broader repository checks. This subtask did not rerun the full test suite/build, import the database, assign release IDs or commit changes.
- No new paper or book is claimed as inspected. Existing Core Guidelines references remain expert guidance; the detailed language claims rely on applicable primary draft clauses. These original lessons have not undergone external academic peer review.
- Large specifications were read at relevant sections, not exhaustively end to end. Scope qualifiers and draft versions are retained. Multi-file and module-build discussions are conceptual project exercises, not claimed cross-toolchain build results.

## Practice placement follow-up

The integration review found that the added core practices were initially concentrated in the final part. Moved the two new reflections in every module to the ends of parts one and two, after the relevant teaching, and kept all 22 original reflections unchanged in part three. Every core module now has exactly one explained reflection in each of its three stable parts.

Revised 11 added practice blocks to state their inputs, contracts or object relationships without relying on a later program listing. Moved seven existing prerequisite paragraphs before the earlier checkpoints: special-member declaration conditions, view constness, noexcept, comparator requirements, lexical normalization, named-module declarations, and construction/destruction dispatch. Integrated the already-cited non-reallocating vector append rule into its walkthrough before asking about the old end iterator. No additional source or external technical claim was introduced in this placement pass.

Explanatory text changed from 24,898 to 25,053 words during this follow-up. Part lengths are now 227–508 words using the same count method. Seven contrasting scenarios were moved from part two to part three to retain substantive final-part application alongside the original reflection, limitations and transfer discussion; the added questions now provide earlier opportunities to check understanding. Word counts are descriptive, not a teaching-quality threshold.

| Module | Part 1 checkpoint | Part 2 checkpoint | Prose words by part |
| --- | --- | --- | --- |
| [Initialize values and read deduced types](../../content/units/cpp/types-initialization/01-initialization-and-deduction.json) | A teammate replaces every pair of constructor parentheses with braces for consistency. Explain a concrete review failure using four zero-filled counters. | You need a counter that starts at zero and an alias that can update it. Describe the declarations and explain whether assigning another integer through the alias changes which object it refers to. | 419 / 507 / 236 |
| [Conversions and domain types](../../content/units/cpp/types-initialization/02-conversions-and-domain-types.json) | Design the type boundary for two integer-valued identifiers, BookId and MemberId, that must never be accidentally interchanged. Would two using aliases solve it? | A program stores the result of 9 / 4 in a double and prints 2. Explain the error and where the conversion belongs if the desired result is 2.25. | 407 / 486 / 238 |
| [Parameters, results, and borrowing](../../content/units/cpp/functions-interfaces/01-parameters-and-results.json) | A helper stores a const reference to its argument and uses it after returning. The argument was alive throughout the call. Is that enough, and what two interface changes could remove the hidden obligation? | Trace total on an empty vector and on {2, 0, 3}. Does the const-reference input imply that total can never overflow for another vector? | 423 / 506 / 234 |
| [Overloads and local callable objects](../../content/units/cpp/functions-interfaces/02-overloads-and-lambdas.json) | With only label(int) and label(double), predict the chosen overload for a double variable containing 4.0 and a float variable containing 4.0f. Explain without using the presence of a fractional value. | A report must apply the same threshold to every row even if the settings object changes between rows. Would [&limit] or [limit] express that requirement for an int threshold? Explain the lifetime consequence as well. | 391 / 507 / 266 |
| [Classes that maintain a useful invariant](../../content/units/cpp/classes-value-semantics/01-invariants-and-operations.json) | A teammate adds a public setter that assigns completed directly. What must its contract enforce, and why does making completed private not solve an incorrect setter? | Trace Progress{7} followed by advance(7), advance(0), and advance(1). Give all results and the final completed and remaining values, explaining the zero request. | 407 / 491 / 229 |
| [Copying, moving, and comparing values](../../content/units/cpp/classes-value-semantics/02-copy-move-and-comparison.json) | Two Book values hold the title Atlas and 120 pages after one is copied from the other. Equality compares both fields. Change only the second value’s pages to 121: which objects change, and are they still equal? What if equality compared only the title? | A developer adds a user-declared destructor that merely logs a message and assumes the previously implicit move constructor still exists. Why must that assumption be checked? | 418 / 476 / 255 |
| [Owners, borrowers, and scope](../../content/units/cpp/lifetime-ownership/01-owners-and-borrowers.json) | A vector has size three and capacity for five elements. You build a span over its three integers, then append one integer while preserving the original elements and their addresses. How many elements does the old span describe? What separate issue arises if an append instead relocates the vector’s storage? | A const span<int> and a span<const int> both sound read-only. Which permits changing a viewed integer, and what does const actually constrain in each case? | 430 / 455 / 269 |
| [RAII and exclusive ownership](../../content/units/cpp/lifetime-ownership/02-raii-and-unique-ownership.json) | A function stores a title only until it returns and never transfers ownership. Compare a local string with a unique_ptr<string> for that job, then identify a requirement that could justify the pointer owner. | After ownership moves from first to second, does the string necessarily need to be recreated at another address? Explain what remains true for an existing observer and when its access ends. | 414 / 503 / 240 |
| [Optional results and complete parsing](../../content/units/cpp/error-handling/01-optional-results-and-parsing.json) | Suppose parse_pages returns optional<int>, with a present value meaning a valid page count and absence meaning invalid input. A caller uses parse_pages(text).value_or(0) and stores that number immediately. Which distinction has disappeared, and how could a C++20 interface preserve useful failure messages? | Classify the fields 7, 7x, -7, 007 and -0 under this exact parser. For each rejected field, identify the layer that rejects it rather than calling every failure a bad integer. | 397 / 508 / 229 |
| [Exceptions and state guarantees](../../content/units/cpp/error-handling/02-exceptions-and-state-guarantees.json) | A developer declares the replacement operation replace_pages noexcept while retaining an explicit throw for invalid input. Does this strengthen recoverability for the caller? Explain which failure-reporting and protected-state contracts must instead be reviewed. | Immediately after a successful swap, which vector owns the old target contents? Explain why the cleanup is correct without a manual clear or delete. | 461 / 383 / 281 |
| [Templates as families of typed operations](../../content/units/cpp/templates-generic-programming/01-typed-families.json) | Consider the described larger template taking two const T& arguments and returning the selected T by value. A new type supports < but cannot be copied from the selected const argument. Does it meet the requirements, and why might returning a reference create a different problem? | Predict larger<double>(4, 9.5) and larger<int>(4, 9.5), and explain why the second result cannot retain the fraction merely because the original literal was floating point. | 406 / 501 / 250 |
| [Concepts and explicit requirements](../../content/units/cpp/templates-generic-programming/02-concepts-and-requirements.json) | Should HasSize also require sorting, random access and heap allocation because std::vector has those capabilities? Explain using a hypothetical fixed-size value that exposes a const size query. | You replace const T& with T& in the requires expression but leave print_size unchanged. What unsupported type can now pass the weaker check, and where would the implementation still fail? | 384 / 479 / 235 |
| [Sequence containers and invalidation](../../content/units/cpp/library-data-containers/01-sequences-and-invalidation.json) | An empty vector<int> reserves ten elements of capacity, then resizes to three. What changed at each step, and which positions may now be read? Do you know its exact capacity? | A vector already has enough capacity for another element. Does appending preserve every iterator, including the old end iterator? Explain how to structure a later search. | 406 / 508 / 227 |
| [Keys, records, and alternative values](../../content/units/cpp/library-data-containers/02-keys-and-vocabulary-types.json) | Choose a representation for each: a required title plus page count, a possibly missing page count, and a field containing either a count or a textual status. Explain why one representation should not be used indiscriminately for all three. | Given the titles Cedar, Atlas, Cedar, Atlas, Cedar, derive the ordered map output and the number of map elements. Why is that element count different from the input length? | 414 / 504 / 256 |
| [Algorithms and half-open ranges](../../content/units/cpp/iterators-algorithms-ranges/01-algorithms-and-half-open-ranges.json) | For the vector {2, 4, 6, 9}, describe the half-open range containing only 4 and 6. Name its starting and stopping positions. Would a search restricted to that segment examine 9, and how many even values does the segment contain? | Why is a comparator using <= invalid for sorting two equal integers? Give the corresponding valid relation and explain whether an empty input repairs the comparator’s contract. | 397 / 452 / 269 |
| [Lazy range pipelines](../../content/units/cpp/iterators-algorithms-ranges/02-lazy-range-pipelines.json) | A function builds a filter-and-transform pipeline over its local vector, then must return results usable after that vector is destroyed. Compare returning this borrowing pipeline with returning a vector that owns the collected results. Why is copying the pipeline insufficient? | Change only the selection rule to values greater than four, keeping multiplication by ten. Predict printed values and saved.size. How does an input with no matching values behave? | 404 / 490 / 247 |
| [Text streams and extraction state](../../content/units/cpp/library-services/01-text-streams-and-extraction.json) | Why would reading the title Atlas of Stars with this record parser fail to mean a single title? Describe a format change instead of simply repeating extraction until it succeeds. | Classify Atlas 0, Atlas -1, Atlas 12 extra and Atlas words. Which successful format cases still need a separate domain check? | 404 / 495 / 245 |
| [Time durations and filesystem paths](../../content/units/cpp/library-services/02-time-and-path-values.json) | A relative pathname ../atlas.txt starts with a parent-directory component. A save operation lexically normalizes it and concludes that the file must be inside its intended directory because normalization succeeded. Explain the error in that reasoning and the separate environmental questions. | A 2500ms interval is converted to integral seconds using duration_cast, floor, ceil and round. Predict the four counts and explain the tie in the last case. | 473 / 401 / 250 |
| [Declarations, definitions, and linkage](../../content/units/cpp/program-organization/01-declarations-and-linkage.json) | A client sees the declaration of a function pages_after and compiles to an object file containing a call to it, but the final executable link omits its implementation object. What has succeeded, what remains missing, and why does including the declaration again not provide the missing body? | Two source files each define an unnamed-namespace helper called bonus. Are those intended to denote one shared public helper? Compare this with a declared public library::bonus defined once. | 405 / 483 / 242 |
| [Headers, namespaces, and module boundaries](../../content/units/cpp/program-organization/02-headers-and-modules.json) | A header’s declarations use std::string but compile only if each client happened to include <string> first. What is wrong with that interface boundary, and how would you test a repair? | A teammate renames a header to a module interface filename and assumes clients can import it with any C++20 compiler command. Which separate pieces of work are missing? | 368 / 467 / 286 |
| [Interfaces and virtual dispatch](../../content/units/cpp/runtime-polymorphism/01-interfaces-and-overriding.json) | Inside show, may the code call a Reminder-only days member simply because one caller passes Reminder? Explain the roles of static interface checking and dynamic dispatch. | A base constructor calls a virtual operation expecting the most-derived class to initialize its own message. Why is that an unsafe design assumption even when ordinary later calls dispatch correctly? | 378 / 462 / 267 |
| [Owning a collection of implementations](../../content/units/cpp/runtime-polymorphism/02-owning-polymorphic-objects.json) | A saved pointer to one separately allocated implementation object survives an append that reallocates the vector of unique_ptr owners. Its owning element is then erased without transferring ownership elsewhere. Explain the difference between these events and identify which one ends the object lifetime. | The loop is generalized to permit empty unique_ptr elements. Which line needs an additional precondition check, and does dynamic_cast’s null result make the whole original loop safe automatically? | 393 / 482 / 257 |

The placement validation confirmed all original code/output blocks, original reflections, source records, discovery metadata and stable IDs remain unchanged. `npm run content:check -- cpp` passed again. No database import, release-ID assignment, commit, full test suite or application test was performed by this subtask.
